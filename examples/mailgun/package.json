{
  "name": "fastify-mailgun-example",
  "description": "An example to configure fastify-mail with MailGun for rendering and sending emails.",
  "main": "index.js",
  "scripts": {
    "start": "fastify start index.js -l info -w"
  },
  "dependencies": {
    "fastify": "^4.25.2",
    "fastify-cli": "^6.0.0",
    "nodemailer-mailgun-transport": "^2.1.5",
    "nunjucks": "^3.2.4"
  }
}
